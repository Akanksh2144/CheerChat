import 'package:flutter/material.dart';

class RandomCallScreen extends StatelessWidget {
  const RandomCallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Random Call Screen',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
